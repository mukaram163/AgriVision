# === Data and model files ===
# Ignore local data, but keep model folder (so best_model.pth is tracked via Git LFS)
data/
results/*.pth

# Keep models folder but ignore temporary or training checkpoints
models/*.tmp
models/*.bak
models/__pycache__/

# === Python / Jupyter cache ===
__pycache__/
*.pyc
*.pyo
*.pyd
.ipynb_checkpoints/
*.ipynb_checkpoints/
.DS_Store

# === Virtual environments / OS files ===
env/
venv/
*.env
*.log
*.swp
